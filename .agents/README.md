# Local Agent Context

This directory contains project-specific agent guidance for `颐享健康大讲堂`.

Start with the root `AGENTS.md`, then use skills in `.agents/skills` only when they match the current task.
